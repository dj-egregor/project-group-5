window.addEventListener('load', () => {
  // const left = document.querySelector('.carousel__icon--left');
  // const right = document.querySelector('.carousel__icon--right');
  // const slider = document.querySelector('.carousel__slider');
  // const images = document.querySelectorAll('.carousel__img');
  // // counter
  // let counter = 0;
  // const stepSize = images[0].clientWidth;
  //  console.log(stepSize);
  // slider.style.transform = 'translateX(' = '${-stepSize * counter}px)';
  // right.addEventListener('click'), () => {
  //     slider.classList.add('transformanimation');
  //     counter++;
  //     slider.style.transform = 'translateX(' = '${-stepSize * counter}px)';
  // }
});
